#!/usr/bin/env bash
# =============================================================================
# k1n-secret-leak-sentinel — Pre-commit Hook
# =============================================================================
# Scans staged files for potential secrets before allowing a commit.
# Blocks the commit if any finding meets or exceeds the configured severity.
#
# Installation:
#   cp hooks/pre-commit/k1n-secret-check .git/hooks/pre-commit
#   chmod +x .git/hooks/pre-commit
#
# Configuration (read from environment or .env file in repo root):
#   K1N_FAIL_ON_SEVERITY   - Minimum severity to block commit (default: high)
#   K1N_ENTROPY_ENABLED    - Enable entropy detection (default: true)
#   K1N_ENTROPY_THRESHOLD  - Shannon entropy threshold (default: 4.5)
#   K1N_POLICY_PROFILE     - Policy profile name (default: default)
#
# To bypass in an emergency (use with extreme caution):
#   git commit --no-verify -m "message"
#   # Note: --no-verify skips ALL hooks, not just this one.
# =============================================================================

set -euo pipefail

# ── Colour output ─────────────────────────────────────────────────────────────
RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BOLD='\033[1m'
NC='\033[0m'  # No colour

# ── Configuration ─────────────────────────────────────────────────────────────
FAIL_ON_SEVERITY="${K1N_FAIL_ON_SEVERITY:-high}"
ENTROPY_ENABLED="${K1N_ENTROPY_ENABLED:-true}"
POLICY_PROFILE="${K1N_POLICY_PROFILE:-default}"

# ── Load .env if present ──────────────────────────────────────────────────────
if [ -f ".env" ]; then
    # Source only lines that look like variable assignments; ignore comments and blanks
    while IFS='=' read -r key value; do
        [[ "$key" =~ ^[[:space:]]*# ]] && continue  # Skip comments
        [[ -z "$key" ]] && continue                  # Skip blank lines
        # Strip inline comments from value
        value="${value%%#*}"
        value="${value%"${value##*[![:space:]]}"}"  # Trim trailing whitespace
        export "${key}=${value}"
    done < <(grep -E '^[A-Z_]+=.*' .env 2>/dev/null || true)
fi

# ── Check that k1n-sentinel is installed ─────────────────────────────────────
if ! command -v k1n-sentinel &> /dev/null; then
    echo -e "${YELLOW}[k1n-sentinel] Warning: k1n-sentinel is not in PATH.${NC}"
    echo "  Install it with: pip install k1n-secret-leak-sentinel"
    echo "  Or activate your virtual environment: source .venv/bin/activate"
    echo ""
    echo "  Skipping secret scan. To enforce scanning, install k1n-sentinel."
    exit 0  # Exit 0 so the commit is not blocked by a missing tool
fi

# ── Run the staged-file scan ──────────────────────────────────────────────────
echo -e "${BOLD}[k1n-sentinel] Scanning staged files for secrets...${NC}"
echo "  Policy:            ${POLICY_PROFILE}"
echo "  Fail on severity:  ${FAIL_ON_SEVERITY}"
echo "  Entropy detection: ${ENTROPY_ENABLED}"
echo ""

# Build the scan command
SCAN_CMD="k1n-sentinel scan-staged \
    --fail-on \"${FAIL_ON_SEVERITY}\" \
    --policy-profile \"${POLICY_PROFILE}\""

if [ "${ENTROPY_ENABLED}" = "true" ]; then
    SCAN_CMD="${SCAN_CMD} --entropy"
fi

# Execute the scan and capture the exit code
set +e
eval "${SCAN_CMD}"
SCAN_EXIT_CODE=$?
set -e

# ── Handle the scan result ───────────────────────────────────────────────────
if [ "${SCAN_EXIT_CODE}" -eq 0 ]; then
    echo -e "${GREEN}[k1n-sentinel] No secrets detected. Proceeding with commit.${NC}"
    exit 0

elif [ "${SCAN_EXIT_CODE}" -eq 1 ]; then
    echo ""
    echo -e "${RED}${BOLD}[k1n-sentinel] COMMIT BLOCKED — Potential secrets detected.${NC}"
    echo ""
    echo "  Review the findings above. If they are false positives, you have two options:"
    echo ""
    echo "  1. Add a suppression entry to .k1n-suppressions.yaml:"
    echo "     suppressions:"
    echo "       - file: \"path/to/file\""
    echo "         reason: \"Test fixture — not a real credential\""
    echo ""
    echo "  2. Rotate the secret immediately, then remove it from the staged file."
    echo ""
    echo "  Do NOT use --no-verify unless you have confirmed there are no real secrets."
    echo ""
    exit 1

else
    echo -e "${YELLOW}[k1n-sentinel] Scan returned an unexpected exit code: ${SCAN_EXIT_CODE}.${NC}"
    echo "  The commit will proceed. Investigate the scan output above."
    exit 0
fi
