#!/usr/bin/env bash
# =============================================================================
# k1n-secret-leak-sentinel — Pre-push Hook
# =============================================================================
# Scans commits that are about to be pushed for secrets — catches anything that
# slipped past the pre-commit hook (e.g., --no-verify commits, CI-generated
# commits, or direct pushes from tooling).
#
# Unlike the pre-commit hook which scans staged files, the pre-push hook scans
# the full diff of commits that are new relative to the remote.
#
# Installation:
#   cp hooks/pre-push/k1n-secret-check-push .git/hooks/pre-push
#   chmod +x .git/hooks/pre-push
#
# Configuration (read from environment):
#   K1N_FAIL_ON_SEVERITY   - Minimum severity to block push (default: high)
#   K1N_ENTROPY_ENABLED    - Enable entropy detection (default: true)
#   K1N_SCAN_DEPTH         - Max commits to scan in a push batch (default: 50)
#
# The hook reads from stdin (format: <local_ref> <local_sha1> <remote_ref> <remote_sha1>)
# as required by the git pre-push hook specification.
# =============================================================================

set -euo pipefail

RED='\033[0;31m'
YELLOW='\033[1;33m'
GREEN='\033[0;32m'
BOLD='\033[1m'
NC='\033[0m'

FAIL_ON_SEVERITY="${K1N_FAIL_ON_SEVERITY:-high}"
ENTROPY_ENABLED="${K1N_ENTROPY_ENABLED:-true}"
SCAN_DEPTH="${K1N_SCAN_DEPTH:-50}"
ZERO_SHA="0000000000000000000000000000000000000000"

# Check that k1n-sentinel is installed
if ! command -v k1n-sentinel &> /dev/null; then
    echo -e "${YELLOW}[k1n-sentinel] Warning: k1n-sentinel not in PATH — skipping push scan.${NC}"
    exit 0
fi

echo -e "${BOLD}[k1n-sentinel] Pre-push secret scan starting...${NC}"
echo "  Fail on severity: ${FAIL_ON_SEVERITY}"
echo "  Entropy:          ${ENTROPY_ENABLED}"
echo "  Max commits:      ${SCAN_DEPTH}"
echo ""

FOUND_SECRETS=0

# Read the push refs from stdin
while read -r local_ref local_sha remote_ref remote_sha; do
    # Deletion push — nothing to scan
    if [ "${local_sha}" = "${ZERO_SHA}" ]; then
        continue
    fi

    # New branch push (no remote SHA yet) — scan from HEAD back SCAN_DEPTH commits
    if [ "${remote_sha}" = "${ZERO_SHA}" ]; then
        RANGE="HEAD~${SCAN_DEPTH}..${local_sha}"
    else
        RANGE="${remote_sha}..${local_sha}"
    fi

    # Get the list of commits in this push range
    COMMITS=$(git log --pretty=format:"%H" "${RANGE}" 2>/dev/null | head -n "${SCAN_DEPTH}" || true)

    if [ -z "${COMMITS}" ]; then
        continue
    fi

    COMMIT_COUNT=$(echo "${COMMITS}" | wc -l | tr -d ' ')
    echo "  Scanning ${COMMIT_COUNT} commit(s) in ${local_ref} → ${remote_ref}"

    for COMMIT in ${COMMITS}; do
        # Generate the diff for this commit (staged content only)
        DIFF=$(git show --stat --patch --no-color "${COMMIT}" 2>/dev/null || true)

        if [ -z "${DIFF}" ]; then
            continue
        fi

        # Write diff to a temp file for scanning
        TMPFILE=$(mktemp /tmp/k1n-push-scan-XXXXXX.diff)
        echo "${DIFF}" > "${TMPFILE}"

        SCAN_CMD="k1n-sentinel scan-file \"${TMPFILE}\" --fail-on \"${FAIL_ON_SEVERITY}\""
        if [ "${ENTROPY_ENABLED}" = "true" ]; then
            SCAN_CMD="${SCAN_CMD} --entropy"
        fi

        set +e
        eval "${SCAN_CMD}"
        SCAN_EXIT="${?}"
        set -e

        rm -f "${TMPFILE}"

        if [ "${SCAN_EXIT}" -eq 1 ]; then
            FOUND_SECRETS=1
            echo -e "${RED}  FAIL: Potential secret detected in commit ${COMMIT:0:8}${NC}"
            echo ""
        fi
    done
done

if [ "${FOUND_SECRETS}" -eq 1 ]; then
    echo ""
    echo -e "${RED}${BOLD}[k1n-sentinel] PUSH BLOCKED — Potential secrets detected in commits.${NC}"
    echo ""
    echo "  Steps to resolve:"
    echo "  1. Identify the finding from the output above"
    echo "  2. If it is a real secret:"
    echo "     a. Rotate the secret immediately"
    echo "     b. Remove it from git history: git rebase -i or BFG Repo Cleaner"
    echo "     c. Force-push only after history is clean (coordinate with team)"
    echo "  3. If it is a false positive:"
    echo "     a. Add a suppression entry to .k1n-suppressions.yaml"
    echo "     b. Document the reason clearly"
    echo ""
    echo "  Do NOT bypass this check without confirming no real secrets are present."
    echo ""
    exit 1
fi

echo -e "${GREEN}[k1n-sentinel] No secrets detected. Push proceeding.${NC}"
exit 0
